{% extends 'partials/base.html.twig' %}

{% block content %}

	<h1>{{ page.title}}</h1>
	<h2>{{ event.startDate|date('l, F d, Y g:i') }}-{{ event.endDate|date('g:ia') }}</h2>

	{{ page.content }}

{% endblock %}
