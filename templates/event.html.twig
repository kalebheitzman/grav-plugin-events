{% extends 'partials/base.html.twig' %}

{% block content %}

	<h1>{{ page.title}}</h1>
	<h2>{{ event.start|date('l, F d, Y, g:i') }}-{{ event.end|date('g:ia') }}</h2>

	{{ page.content }}

{% endblock %}
